[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

# Переменные окружения (установите в Netlify Dashboard)
# NEXT_PUBLIC_API_BASE_URL будет установлена через Environment variables в Netlify

