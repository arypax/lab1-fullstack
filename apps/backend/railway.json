{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "poetry run alembic upgrade head || true; poetry run uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-4000}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

