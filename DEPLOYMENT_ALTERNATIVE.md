# Альтернативный деплой без AWS

Это руководство описывает процесс деплоя приложения **без использования AWS**, используя бесплатные альтернативные платформы.

## 🎯 Рекомендуемый стек (бесплатный)

- **База данных**: [Supabase](https://supabase.com/) или [Neon](https://neon.tech/) (PostgreSQL)
- **Backend**: [Railway](https://railway.app/) или [Render](https://render.com/)
- **Frontend**: [Vercel](https://vercel.com/) или [Netlify](https://www.netlify.com/)

---

## Вариант 1: Supabase + Railway + Vercel (Рекомендуется)

### Шаг 1: Создание базы данных в Supabase

1. Зарегистрируйтесь на [supabase.com](https://supabase.com/) (бесплатно, только email)
2. Создайте новый проект:
   - Нажмите **New Project**
   - Выберите организацию (или создайте новую)
   - Введите имя проекта: `lab1-database`
   - Введите пароль для базы данных (сохраните его!)
   - Выберите регион (ближайший к вам)
   - Нажмите **Create new project**

3. Дождитесь создания проекта (1-2 минуты)

4. Получите данные подключения (см. подробную инструкцию в [SUPABASE_CONNECTION_GUIDE.md](./SUPABASE_CONNECTION_GUIDE.md)):

   **Способ 1 (через Project Settings):**
   - В левом меню найдите иконку **шестеренки** (⚙️) внизу
   - Нажмите на неё → выберите **Database**
   - Прокрутите до секции **Connection string** или **Connection info**
   - Скопируйте строку подключения в формате **URI**
   
   **Способ 2 (через кнопку Connect):**
   - В верхней панели нажмите кнопку **Connect**
   - В открывшемся окне найдите строку подключения для Python/psycopg2
   
   **Способ 3 (через SQL Editor):**
   - В левом меню выберите **SQL Editor**
   - В правом верхнем углу найдите иконку подключения

5. **Важно:** В Supabase используется база данных `postgres` по умолчанию. Не нужно создавать отдельную базу `lab1db` - просто используйте базу `postgres` и создавайте таблицы через миграции Alembic.

6. Формат Connection string для вашего приложения:

   **Вариант 1: Direct connection (для локальной разработки):**
   ```
   postgresql+psycopg2://postgres:[YOUR_PASSWORD]@db.rivtrxzjpnxbgfmjpwha.supabase.co:5432/postgres
   ```
   
   ⚠️ **Внимание:** Direct connection может не работать на некоторых платформах (Railway, Render) из-за IPv4/IPv6. Используйте Pooler для продакшена.
   
   **Вариант 2: Session Pooler (рекомендуется для деплоя):**
   - В окне "Connect to your project" переключите **Method** на **Session Pooler**
   - Или нажмите кнопку **Pooler settings** в предупреждении
   - Скопируйте строку подключения (будет выглядеть примерно так):
   ```
   postgresql+psycopg2://postgres.rivtrxzjpnxbgfmjpwha:[YOUR_PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
   ```
   
   **Замените `[YOUR_PASSWORD]` на ваш реальный пароль!**
   
   > 💡 **Важно:** Для деплоя на Railway/Render используйте **Session Pooler** (порт 6543), так как он совместим с IPv4 и более эффективен.

### Шаг 2: Деплой Backend на Railway

1. Зарегистрируйтесь на [railway.app](https://railway.app/) (через GitHub)

2. Создайте новый проект:
   - Нажмите **New Project**
   - Выберите **Deploy from GitHub repo**
   - Выберите ваш репозиторий
   - Выберите папку `apps/backend`

3. Настройте переменные окружения:
   - Перейдите в **Variables**
   - Добавьте:
     ```
     DATABASE_URL=postgresql+psycopg2://postgres.rivtrxzjpnxbgfmjpwha:[YOUR_PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
     CORS_ORIGINS=https://your-frontend.vercel.app,http://localhost:3000
     PORT=4000
     ```
   
   ⚠️ **Важно:** 
   - Замените `[YOUR_PASSWORD]` на ваш реальный пароль из Supabase
   - Используйте **Session Pooler** (порт 6543) вместо Direct connection (порт 5432) для совместимости с Railway/Render
   - Если не знаете Pooler URL, вернитесь в Supabase → Connect → переключите Method на "Session Pooler"

4. Railway автоматически определит Python и установит зависимости из `pyproject.toml`

5. Настройте команду запуска:
   - Перейдите в **Settings** → **Deploy**
   - В **Start Command** укажите:
     ```
     poetry run uvicorn app.main:app --host 0.0.0.0 --port $PORT
     ```

6. Дождитесь деплоя и скопируйте URL вашего API

### Шаг 3: Деплой Frontend на Vercel

1. Зарегистрируйтесь на [vercel.com](https://vercel.com/) (через GitHub)

2. Импортируйте проект:
   - Нажмите **Add New** → **Project**
   - Выберите ваш GitHub репозиторий
   - В **Root Directory** укажите: `apps/frontend`

3. Настройте проект:
   - **Framework Preset**: Next.js
   - **Build Command**: `npm run build` (или оставьте по умолчанию)
   - **Output Directory**: `.next` (или оставьте по умолчанию)

4. Добавьте переменные окружения:
   - В разделе **Environment Variables** добавьте:
     ```
     NEXT_PUBLIC_API_BASE_URL=https://your-backend.railway.app
     ```

5. Нажмите **Deploy**

6. После деплоя скопируйте URL вашего frontend

7. Обновите CORS в backend:
   - Вернитесь в Railway
   - Обновите переменную `CORS_ORIGINS` с URL вашего Vercel сайта

---

## Вариант 2: Neon + Render + Netlify

### Шаг 1: Создание базы данных в Neon

1. Зарегистрируйтесь на [neon.tech](https://neon.tech/) (бесплатно, только email)

2. Создайте новый проект:
   - Нажмите **Create Project**
   - Введите имя: `lab1-database`
   - Выберите регион
   - Нажмите **Create Project**

3. Получите Connection string:
   - После создания проекта скопируйте **Connection string**
   - Формат: `postgresql://user:password@ep-xxxxx.region.aws.neon.tech/neondb?sslmode=require`

4. Обновите Connection string для вашего приложения:
   ```
   postgresql+psycopg2://user:password@ep-xxxxx.region.aws.neon.tech/neondb?sslmode=require
   ```

### Шаг 2: Деплой Backend на Render

1. Зарегистрируйтесь на [render.com](https://render.com/) (через GitHub)

2. Создайте новый Web Service:
   - Нажмите **New** → **Web Service**
   - Подключите ваш GitHub репозиторий
   - Выберите папку `apps/backend`

3. Настройте сервис:
   - **Name**: `lab1-backend`
   - **Environment**: `Python 3`
   - **Build Command**: `poetry install && poetry run alembic upgrade head`
   - **Start Command**: `poetry run uvicorn app.main:app --host 0.0.0.0 --port $PORT`

4. Добавьте переменные окружения:
   ```
   DATABASE_URL=postgresql+psycopg2://user:password@ep-xxxxx.region.aws.neon.tech/neondb?sslmode=require
   CORS_ORIGINS=https://your-site.netlify.app,http://localhost:3000
   PORT=10000
   ```

5. Нажмите **Create Web Service**

6. Дождитесь деплоя (5-10 минут)

### Шаг 3: Деплой Frontend на Netlify

1. Зарегистрируйтесь на [netlify.com](https://www.netlify.com/) (через GitHub)

2. Импортируйте проект:
   - Нажмите **Sites** → **Import an existing project**
   - Выберите **Deploy with GitHub**
   - Выберите ваш репозиторий

3. Настройте сборку:
   - **Base directory**: `apps/frontend`
   - **Build command**: `npm run build`
   - **Publish directory**: `.next`

4. Добавьте переменные окружения:
   ```
   NEXT_PUBLIC_API_BASE_URL=https://your-backend.onrender.com
   ```

5. Нажмите **Deploy site**

---

## Вариант 3: Полный деплой на Railway (все в одном месте)

Railway позволяет деплоить все сервисы в одном проекте.

### Шаг 1: База данных на Railway

1. В Railway создайте новый проект
2. Нажмите **New** → **Database** → **Add PostgreSQL**
3. Railway автоматически создаст базу данных
4. Скопируйте **DATABASE_URL** из переменных окружения

### Шаг 2: Backend на Railway

1. В том же проекте нажмите **New** → **GitHub Repo**
2. Выберите папку `apps/backend`
3. Добавьте переменные окружения:
   ```
   DATABASE_URL=<скопированный из базы данных>
   CORS_ORIGINS=https://your-frontend.railway.app,http://localhost:3000
   PORT=4000
   ```
4. Railway автоматически определит Python и установит зависимости

### Шаг 3: Frontend на Railway

1. В том же проекте нажмите **New** → **GitHub Repo**
2. Выберите папку `apps/frontend`
3. Добавьте переменные окружения:
   ```
   NEXT_PUBLIC_API_BASE_URL=https://your-backend.railway.app
   ```
4. Railway автоматически определит Next.js

---

## Применение миграций базы данных

После деплоя backend нужно применить миграции Alembic.

### Для Railway:

1. Перейдите в ваш backend сервис
2. Откройте **Deploy Logs**
3. В **Settings** → **Deploy** добавьте в **Start Command**:
   ```
   poetry run alembic upgrade head && poetry run uvicorn app.main:app --host 0.0.0.0 --port $PORT
   ```

### Для Render:

1. В **Build Command** добавьте:
   ```
   poetry install && poetry run alembic upgrade head
   ```

### Альтернатива: Применить миграции вручную

1. Подключитесь к базе данных через psql или pgAdmin
2. Выполните миграции вручную или используйте локальный Alembic:
   ```bash
   cd apps/backend
   DATABASE_URL=your_production_database_url poetry run alembic upgrade head
   ```

---

## Сравнение платформ

| Платформа | База данных | Backend | Frontend | Бесплатный тариф |
|-----------|-------------|---------|----------|------------------|
| **Supabase** | ✅ PostgreSQL | ❌ | ❌ | 500 MB БД, 2 GB bandwidth |
| **Neon** | ✅ PostgreSQL | ❌ | ❌ | 0.5 GB БД |
| **Railway** | ✅ PostgreSQL | ✅ Python | ✅ Node.js | $5 кредит/месяц |
| **Render** | ✅ PostgreSQL | ✅ Python | ✅ Static | 750 часов/месяц |
| **Vercel** | ❌ | ❌ | ✅ Next.js | Unlimited |
| **Netlify** | ❌ | ❌ | ✅ Next.js | 100 GB bandwidth |

---

## Рекомендации

### Для учебного проекта (бесплатно):
1. **База данных**: Supabase или Neon
2. **Backend**: Railway (если есть кредит) или Render
3. **Frontend**: Vercel (лучше для Next.js) или Netlify

### Для продакшена:
- Используйте платные тарифы для стабильности
- Настройте резервное копирование базы данных
- Используйте CDN для frontend

---

## Полезные ссылки

- [Supabase Documentation](https://supabase.com/docs)
- [Neon Documentation](https://neon.tech/docs)
- [Railway Documentation](https://docs.railway.app/)
- [Render Documentation](https://render.com/docs)
- [Vercel Documentation](https://vercel.com/docs)
- [Netlify Documentation](https://docs.netlify.com/)

---

## Важные замечания

⚠️ **Безопасность**:
- Никогда не коммитьте `.env` файлы
- Используйте переменные окружения в облачных сервисах
- Регулярно обновляйте пароли

⚠️ **Ограничения бесплатных тарифов**:
- Supabase: 500 MB БД, может засыпать после неактивности
- Neon: 0.5 GB БД, автоматическая пауза после неактивности
- Railway: $5 кредит/месяц, может закончиться
- Render: 750 часов/месяц, сервисы могут засыпать

⚠️ **Резервное копирование**:
- Настройте автоматические снапшоты базы данных
- Регулярно делайте экспорт данных

