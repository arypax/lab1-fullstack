[build]
  base = "apps/frontend"
  command = "cd apps/frontend && npm install --legacy-peer-deps --package-lock-only && npm run build"
  publish = "apps/frontend/.next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

