[build]
  base = "."
  command = "pnpm --filter @lab1/frontend run build"
  publish = "apps/frontend/.next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  # Используем настройки npm/lock-файл по умолчанию

