# Выполните эти команды после авторизации в GitHub:

# 1. Авторизуйтесь (откроется браузер):
gh auth login --web

# 2. Создайте репозиторий и загрузите код:
gh repo create lab1-fullstack --public --source=. --remote=origin --push

# ИЛИ если репозиторий уже создан вручную:
# git remote add origin https://github.com/YOUR_USERNAME/lab1-fullstack.git
# git push -u origin main

